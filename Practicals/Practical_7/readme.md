Create a webpage to practice DOM manipulation using JavaScript with
A heading and some paragraphs
A container section with elements inside it
Buttons that perform the following DOM actions:
Change the text of an existing element
Add a new element to the page
Remove the last added element
Link an external JavaScript file and use DOM methods such as: